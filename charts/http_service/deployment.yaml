apiVersion: apps/v1
kind: Deployment
metadata:
  name: http-service
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: http-service
  template:
    metadata:
      labels:
        app: http-service
    spec:
      containers:
        - name: http-service
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: 3030
